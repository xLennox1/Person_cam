/* ============================================================
   PersonCam – DOM-Referenzen
   ============================================================ */
const pinScreen=document.getElementById('pinScreen');const pinInput=document.getElementById('pinInput');const pinError=document.getElementById('pinError');const pinSubmit=document.getElementById('pinSubmit');
const appRoot=document.getElementById('app');const video=document.getElementById('preview');const canvas=document.getElementById('overlay');const ctx=canvas.getContext('2d');const statusEl=document.getElementById('status');const recBadge=document.getElementById('recBadge');const recTimerEl=document.getElementById('recTimer');const startBtn=document.getElementById('start');const stopBtn=document.getElementById('stop');
const settingsBtn=document.getElementById('settingsBtn');const lockBtn=document.getElementById('lockBtn');const settingsOverlay=document.getElementById('settingsOverlay');const closeSettingsBtn=document.getElementById('closeSettings');
const graceInput=document.getElementById('grace');const sensitivityInput=document.getElementById('sensitivity');const trackingEnabledInput=document.getElementById('trackingEnabled');const faceSnapshotsEnabledInput=document.getElementById('faceSnapshotsEnabled');const faceMinConfidenceInput=document.getElementById('faceMinConfidence');const faceCooldownInput=document.getElementById('faceCooldown');
const changePinBtn=document.getElementById('changePinBtn');const changePinForm=document.getElementById('changePinForm');const currentPinInput=document.getElementById('currentPin');const newPinInput=document.getElementById('newPin');const newPin2Input=document.getElementById('newPin2');const pinChangeMsg=document.getElementById('pinChangeMsg');const savePinBtn=document.getElementById('savePinBtn');const lockNowBtn=document.getElementById('lockNowBtn');
const tabBtns=document.querySelectorAll('.tab-btn');const recordingsPanel=document.getElementById('recordingsPanel');const facesPanel=document.getElementById('facesPanel');const recordingsList=document.getElementById('recordings');const recordingsEmpty=document.getElementById('recordingsEmpty');const clearVideosBtn=document.getElementById('clearVideos');const faceGallery=document.getElementById('faceGallery');const facesEmpty=document.getElementById('facesEmpty');const clearFacesBtn=document.getElementById('clearFaces');
const lightbox=document.getElementById('lightbox');const lightboxImg=document.getElementById('lightboxImg');const lightboxDownload=document.getElementById('lightboxDownload');const lightboxDelete=document.getElementById('lightboxDelete');const closeLightboxBtn=document.getElementById('closeLightbox');
const MODEL_BASE='lite_mobilenet_v2';const DETECT_INTERVAL_MS=300;const CONFIRM_HITS=2;const MIN_BOX_HEIGHT_RATIO=0.04;const MAX_CONSECUTIVE_ERRORS=5;const TRACK_TIMEOUT_MS=2000;const TRACK_MATCH_DIST=0.25;const DB_NAME='personcam-db';const DB_VERSION=1;const STORE_VIDEOS='videos';const STORE_FACES='faces';const DEFAULT_PIN='1234';const PBKDF2_ITERATIONS=150000;
function setStatus(s){statusEl.textContent=s;}function formatDuration(t){const m=Math.floor(t/60),s=t%60;return`${String(m).padStart(2,'0')}:${String(s).padStart(2,'0')}`;}function formatFilenameStamp(ts){const d=new Date(ts),p=n=>String(n).padStart(2,'0');return`${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}-${p(d.getHours())}-${p(d.getMinutes())}-${p(d.getSeconds())}`;}function bytesToBase64(bytes){let bin='';bytes.forEach(b=>bin+=String.fromCharCode(b));return btoa(bin);}function base64ToBytes(b64){const bin=atob(b64),bytes=new Uint8Array(bin.length);for(let i=0;i<bin.length;i++)bytes[i]=bin.charCodeAt(i);return bytes;}
let dbPromise=null;function getDB(){if(dbPromise)return dbPromise;dbPromise=new Promise((resolve,reject)=>{if(!('indexedDB'in window)){reject(new Error('IndexedDB nicht verfügbar'));return;}const req=indexedDB.open(DB_NAME,DB_VERSION);req.onupgradeneeded=e=>{const db=e.target.result;if(!db.objectStoreNames.contains(STORE_VIDEOS))db.createObjectStore(STORE_VIDEOS,{keyPath:'id'});if(!db.objectStoreNames.contains(STORE_FACES))db.createObjectStore(STORE_FACES,{keyPath:'id'});};req.onsuccess=()=>resolve(req.result);req.onerror=()=>reject(req.error);});return dbPromise;}async function idbPut(store,r){const db=await getDB();return new Promise((res,rej)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).put(r);tx.oncomplete=()=>res();tx.onerror=()=>rej(tx.error);});}async function idbGetAll(store){const db=await getDB();return new Promise((res,rej)=>{const tx=db.transaction(store,'readonly'),req=tx.objectStore(store).getAll();req.onsuccess=()=>res(req.result||[]);req.onerror=()=>rej(req.error);});}async function idbDelete(store,id){const db=await getDB();return new Promise((res,rej)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).delete(id);tx.oncomplete=()=>res();tx.onerror=()=>rej(tx.error);});}async function idbClear(store){const db=await getDB();return new Promise((res,rej)=>{const tx=db.transaction(store,'readwrite');tx.objectStore(store).clear();tx.oncomplete=()=>res();tx.onerror=()=>rej(tx.error);});}
const DEFAULT_SETTINGS={grace:4,sensitivity:55,trackingEnabled:true,faceSnapshotsEnabled:true,faceMinConfidence:70,faceCooldown:5};function loadSettings(){let s={...DEFAULT_SETTINGS};try{const raw=localStorage.getItem('personcam_settings');if(raw)s={...s,...JSON.parse(raw)}}catch(e){console.error(e)}graceInput.value=s.grace;sensitivityInput.value=s.sensitivity;trackingEnabledInput.checked=s.trackingEnabled;faceSnapshotsEnabledInput.checked=s.faceSnapshotsEnabled;faceMinConfidenceInput.value=s.faceMinConfidence;faceCooldownInput.value=s.faceCooldown;}function saveSettings(){const s={grace:Number(graceInput.value)||4,sensitivity:Number(sensitivityInput.value)||55,trackingEnabled:trackingEnabledInput.checked,faceSnapshotsEnabled:faceSnapshotsEnabledInput.checked,faceMinConfidence:Number(faceMinConfidenceInput.value)||70,faceCooldown:Number(faceCooldownInput.value)||5};try{localStorage.setItem('personcam_settings',JSON.stringify(s))}catch(e){console.error(e)}}[graceInput,sensitivityInput,trackingEnabledInput,faceSnapshotsEnabledInput,faceMinConfidenceInput,faceCooldownInput].forEach(el=>el.addEventListener('change',saveSettings));function getThreshold(){const n=Number(sensitivityInput.value);return Number.isNaN(n)?.55:Math.min(.9,Math.max(.3,n/100));}function getFaceMinConfidence(){const n=Number(faceMinConfidenceInput.value);return Number.isNaN(n)?.7:Math.min(.99,Math.max(.3,n/100));}function getFaceCooldownMs(){return Math.max(1,Number(faceCooldownInput.value)||5)*1000;}
async function derivePinHash(pin,salt){const km=await crypto.subtle.importKey('raw',new TextEncoder().encode(pin),'PBKDF2',false,['deriveBits']);const bits=await crypto.subtle.deriveBits({name:'PBKDF2',salt,iterations:PBKDF2_ITERATIONS,hash:'SHA-256'},km,256);return bytesToBase64(new Uint8Array(bits));}async function setPin(pin){const salt=crypto.getRandomValues(new Uint8Array(16));localStorage.setItem('personcam_pin_salt',bytesToBase64(salt));localStorage.setItem('personcam_pin_hash',await derivePinHash(pin,salt));}async function verifyPin(pin){const s=localStorage.getItem('personcam_pin_salt'),h=localStorage.getItem('personcam_pin_hash');return!!(s&&h)&&h===await derivePinHash(pin,base64ToBytes(s));}async function ensurePinProvisioned(){if(!localStorage.getItem('personcam_pin_hash'))await setPin(DEFAULT_PIN);}async function unlockApp(){pinScreen.hidden=true;appRoot.hidden=false;await Promise.all([renderRecordings(),renderFaces()]);}function lockApp(){closeSettingsSheet();if(running)stopCamera();appRoot.hidden=true;pinInput.value='';pinError.hidden=true;pinScreen.hidden=false;pinInput.focus();}async function handlePinSubmit(){const v=pinInput.value.trim();if(!v)return;try{if(await verifyPin(v)){pinInput.value='';pinError.hidden=true;await unlockApp();}else{pinError.textContent='Falsche PIN.';pinError.hidden=false;pinInput.value='';pinInput.focus();}}catch(e){console.error(e);pinError.textContent='PIN-Prüfung nicht möglich (HTTPS/localhost erforderlich).';pinError.hidden=false;}}pinSubmit.addEventListener('click',handlePinSubmit);pinInput.addEventListener('keydown',e=>{if(e.key==='Enter')handlePinSubmit()});lockBtn.addEventListener('click',lockApp);lockNowBtn.addEventListener('click',lockApp);changePinBtn.addEventListener('click',()=>{changePinForm.hidden=!changePinForm.hidden;pinChangeMsg.hidden=true;currentPinInput.value='';newPinInput.value='';newPin2Input.value=''});function showPinChangeMsg(t,ok){pinChangeMsg.textContent=t;pinChangeMsg.style.color=ok?'var(--green)':'var(--red)';pinChangeMsg.hidden=false;}savePinBtn.addEventListener('click',async()=>{const c=currentPinInput.value.trim(),n=newPinInput.value.trim(),n2=newPin2Input.value.trim();if(!c||!n||!n2){showPinChangeMsg('Bitte alle Felder ausfüllen.');return}if(n.length<4){showPinChangeMsg('Neue PIN muss mindestens 4 Zeichen haben.');return}if(n!==n2){showPinChangeMsg('Neue PIN stimmt nicht überein.');return}try{if(!(await verifyPin(c))){showPinChangeMsg('Aktuelle PIN ist falsch.');return}await setPin(n);currentPinInput.value='';newPinInput.value='';newPin2Input.value='';showPinChangeMsg('PIN wurde geändert.',true)}catch(e){showPinChangeMsg('PIN konnte nicht geändert werden.')}});
function createTracker(){let nextId=1,slots=[];return{update(boxes,w,h){const now=performance.now(),used=new Set(),out=[];for(const b of boxes){const cx=b[0]+b[2]/2,cy=b[1]+b[3]/2;let best=null,bd=Infinity;for(const s of slots){if(used.has(s))continue;const dx=(cx-s.cx)/w,dy=(cy-s.cy)/h,d=Math.hypot(dx,dy);if(d<bd){bd=d;best=s}}const s=best&&bd<=TRACK_MATCH_DIST?best:{id:nextId++,cx,cy,lastSeen:now};s.cx=cx;s.cy=cy;s.lastSeen=now;if(!slots.includes(s))slots.push(s);used.add(s);out.push({box:b,id:s.id});}slots=slots.filter(s=>now-s.lastSeen<=TRACK_TIMEOUT_MS);return out},reset(){slots=[];nextId=1}}}const personTracker=createTracker(),faceTracker=createTracker();
let stream,cocoModel,faceModel,running=false,lastVideoTime=-1,lastDetectAt=0,detecting=false,personStreak=0,consecutiveErrors=0;const faceSnapshotTimes=new Map();
function getModelErrorMessage(e){const t=e?.message||String(e||'Unbekannter Fehler');if(/failed to fetch|network|load|404|cors|internet/i.test(t))return'KI-Modelle konnten nicht aus dem Internet geladen werden. Bitte Internetverbindung prüfen und erneut versuchen.';if(/webgl|shader|texture|backend/i.test(t))return'Die Grafikbeschleunigung dieses Geräts konnte nicht verwendet werden. Bitte erneut versuchen.';return`KI-Modelle konnten nicht geladen werden: ${t}`;}
async function prepareTensorFlow(){if(typeof tf==='undefined')throw new Error('TensorFlow.js wurde nicht geladen.');try{if(tf.getBackend()!=='webgl')await tf.setBackend('webgl');await tf.ready();}catch(e){console.warn('WebGL nicht verfügbar, CPU wird verwendet.',e);try{await tf.setBackend('cpu');await tf.ready()}catch(e2){throw new Error(`TensorFlow-Backend konnte nicht gestartet werden: ${e2?.message||e2}`)}}}
async function loadModels(){await prepareTensorFlow();if(!cocoModel)cocoModel=await cocoSsd.load({base:MODEL_BASE});if(!faceModel)faceModel=await blazeface.load();}
async function startCamera(){if(running)return;try{if(!navigator.mediaDevices?.getUserMedia)throw new Error('Kamera-API ist in diesem Browser nicht verfügbar.');stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'},width:{ideal:1280},height:{ideal:720}},audio:true});}catch(e){console.error(e);let m='Bitte erlaube den Kamera-/Mikrofonzugriff. Die Seite muss über HTTPS oder localhost laufen.';if(e.name==='NotFoundError')m='Keine Kamera gefunden.';else if(e.name==='NotReadableError')m='Die Kamera wird bereits von einer anderen App verwendet.';else if(e.name==='NotAllowedError')m='Kamera-/Mikrofonzugriff wurde verweigert.';else if(e.name==='SecurityError')m='Kamera darf auf dieser Seite nicht verwendet werden. Bitte HTTPS verwenden.';else if(e.message)m=e.message;setStatus('Kamera konnte nicht gestartet werden');alert(m);return}video.srcObject=stream;await video.play();running=true;startBtn.disabled=true;stopBtn.disabled=false;try{setStatus('Kamera läuft – KI-Modelle werden geladen…');await loadModels();personStreak=0;detecting=false;consecutiveErrors=0;personTracker.reset();faceTracker.reset();faceSnapshotTimes.clear();setStatus('Bereit – Personen werden erkannt');detectLoop()}catch(e){console.error('KI-Modelle konnten nicht geladen werden:',e);running=false;startBtn.disabled=false;stopBtn.disabled=true;stream?.getTracks().forEach(t=>t.stop());stream=null;video.srcObject=null;setStatus('KI-Modelle konnten nicht geladen werden');alert(getModelErrorMessage(e));}}
function stopCamera(){running=false;clearTimeout(stopTimer);stopTimer=null;if(recording)stopRecording();stream?.getTracks().forEach(t=>t.stop());stream=null;video.srcObject=null;ctx.clearRect(0,0,canvas.width,canvas.height);startBtn.disabled=false;stopBtn.disabled=true;personStreak=0;detecting=false;lastDetectAt=0;consecutiveErrors=0;personTracker.reset();faceTracker.reset();setStatus('Gestoppt');}
function boxIsBigEnough(b,h){return b[3]/h>=MIN_BOX_HEIGHT_RATIO;}async function detectLoop(){if(!running||!cocoModel||!faceModel)return;const now=performance.now(),nf=video.readyState>=2&&video.currentTime!==lastVideoTime;if(nf&&!detecting&&now-lastDetectAt>=DETECT_INTERVAL_MS){lastVideoTime=video.currentTime;lastDetectAt=now;detecting=true;try{const w=video.videoWidth||1280,h=video.videoHeight||720,th=getThreshold();const[preds,faces]=await Promise.all([cocoModel.detect(video),faceModel.estimateFaces(video,false)]);const people=preds.filter(p=>p.class==='person'&&p.score>=th&&boxIsBigEnough(p.bbox,h));const faceBoxes=faces.map(f=>{const[x1,y1]=f.topLeft,[x2,y2]=f.bottomRight,score=Array.isArray(f.probability)?f.probability[0]:f.probability;return{bbox:[x1,y1,x2-x1,y2-y1],score}}).filter(f=>f.score>=getFaceMinConfidence());const tp=trackingEnabledInput.checked?personTracker.update(people.map(p=>p.bbox),w,h):people.map(p=>({box:p.bbox,id:null}));const tfc=faceTracker.update(faceBoxes.map(f=>f.bbox),w,h);drawHud(people,tp,faceBoxes,tfc);maybeCaptureFaceSnapshots(faceBoxes,tfc);if(people.length){if(stopTimer){clearTimeout(stopTimer);stopTimer=null}personStreak++;if(!recording){if(personStreak>=CONFIRM_HITS)startRecording();else setStatus(`Person erkannt – bestätige… (${personStreak}/${CONFIRM_HITS})`)}}else{if(personStreak>0&&!recording)setStatus('Bereit – Personen werden erkannt');personStreak=0;if(recording&&!stopTimer){const sec=Math.max(1,Number(graceInput.value)||4);stopTimer=setTimeout(()=>{stopTimer=null;stopRecording()},sec*1000)}}consecutiveErrors=0}catch(e){console.error('Erkennung fehlgeschlagen:',e);if(++consecutiveErrors>=MAX_CONSECUTIVE_ERRORS)setStatus('Erkennung läuft instabil – Seite bei Bedarf neu laden')}finally{detecting=false}}requestAnimationFrame(detectLoop)}
function drawBox(x,y,w,h,label,color){const lw=ctx.measureText(label).width+12;ctx.strokeStyle=color;ctx.lineWidth=Math.max(2,canvas.width/450);ctx.strokeRect(x,y,w,h);ctx.fillStyle=color;ctx.fillRect(x,Math.max(0,y-22),lw,20);ctx.fillStyle='#000';ctx.fillText(label,x+6,Math.max(15,y-7));}function drawHud(people,tp,faceBoxes,tf){canvas.width=video.videoWidth||1280;canvas.height=video.videoHeight||720;ctx.clearRect(0,0,canvas.width,canvas.height);ctx.font='bold 14px ui-monospace, Consolas, monospace';tp.forEach((t,i)=>{const p=people[i],[x,y,w,h]=t.box,label=trackingEnabledInput.checked&&t.id?`PERSON ${t.id} ${Math.round(p.score*100)}%`:`PERSON ${Math.round(p.score*100)}%`;drawBox(x,y,w,h,label,'#00ff6a')});tf.forEach((t,i)=>{const f=faceBoxes[i],[x,y,w,h]=t.box;drawBox(x,y,w,h,`GESICHT ${Math.round(f.score*100)}%`,'#7dffb0')});}
let recorder,chunks=[],recording=false,stopTimer=null,recordingStartedAt=0,recTimerInterval=null;function startRecording(){if(!stream||recording)return;chunks=[];const mime=['video/webm;codecs=vp9,opus','video/webm;codecs=vp8,opus','video/webm','video/mp4'].find(m=>window.MediaRecorder&&MediaRecorder.isTypeSupported(m));if(!window.MediaRecorder||!mime){setStatus('Dieser Browser unterstützt keine Videoaufnahme.');return}recorder=new MediaRecorder(stream,{mimeType:mime});recorder.ondataavailable=e=>{if(e.data.size)chunks.push(e.data)};recorder.onstop=saveRecording;recorder.start(500);recording=true;recordingStartedAt=Date.now();recBadge.style.display='block';updateRecTimer();recTimerInterval=setInterval(updateRecTimer,1000);setStatus('Person erkannt – Aufnahme läuft');}function updateRecTimer(){recTimerEl.textContent=formatDuration(Math.floor((Date.now()-recordingStartedAt)/1000));}function stopRecording(){if(!recorder||recorder.state==='inactive')return;recorder.stop();recording=false;clearInterval(recTimerInterval);recTimerInterval=null;recBadge.style.display='none';setStatus('Aufnahme wird vorbereitet…');}async function saveRecording(){const dur=Math.max(1,Math.round((Date.now()-recordingStartedAt)/1000)),blob=new Blob(chunks,{type:recorder.mimeType||'video/webm'});chunks=[];try{await idbPut(STORE_VIDEOS,{id:`video-${Date.now()}`,blob,mimeType:recorder.mimeType||'video/webm',createdAt:Date.now(),durationSeconds:dur});await renderRecordings();setStatus('Aufnahme gespeichert')}catch(e){console.error(e);setStatus('Aufnahme konnte nicht gespeichert werden')}}
async function maybeCaptureFaceSnapshots(boxes,tracked){if(!faceSnapshotsEnabledInput.checked||!boxes.length)return;const now=Date.now(),cool=getFaceCooldownMs();for(let i=0;i<boxes.length;i++){const id=tracked[i]?.id??`face-${i}`;if(now-(faceSnapshotTimes.get(id)||0)<cool)continue;const f=boxes[i],[x,y,w,h]=f.bbox,px=w*.35,py=h*.45,sx=Math.max(0,Math.floor(x-px)),sy=Math.max(0,Math.floor(y-py)),ex=Math.min(video.videoWidth,Math.ceil(x+w+px)),ey=Math.min(video.videoHeight,Math.ceil(y+h+py)),sw=Math.max(1,ex-sx),sh=Math.max(1,ey-sy),crop=document.createElement('canvas');crop.width=sw;crop.height=sh;crop.getContext('2d').drawImage(video,sx,sy,sw,sh,0,0,sw,sh);try{const blob=await new Promise(r=>crop.toBlob(r,'image/jpeg',.9));if(!blob)continue;await idbPut(STORE_FACES,{id:`face-${Date.now()}-${Math.random().toString(36).slice(2,8)}`,blob,createdAt:Date.now(),confidence:f.score});faceSnapshotTimes.set(id,now);await renderFaces()}catch(e){console.error(e)}}}
async function renderRecordings(){const items=await idbGetAll(STORE_VIDEOS);items.sort((a,b)=>b.createdAt-a.createdAt);recordingsList.innerHTML='';recordingsEmpty.hidden=items.length>0;for(const item of items){const card=document.createElement('article');card.className='media-card';const url=URL.createObjectURL(item.blob);card.innerHTML=`<video controls playsinline preload="metadata" src="${url}"></video><div class="media-meta"><span>${new Date(item.createdAt).toLocaleString('de-DE')}</span><span>${formatDuration(item.durationSeconds||0)}</span></div><div class="media-actions"><a class="btn btn-ghost" href="${url}" download="PersonCam-${formatFilenameStamp(item.createdAt)}.webm">Herunterladen</a><button class="btn btn-ghost delete-video">Löschen</button></div>`;card.querySelector('.delete-video').addEventListener('click',async()=>{await idbDelete(STORE_VIDEOS,item.id);await renderRecordings()});recordingsList.appendChild(card)}}async function renderFaces(){const items=await idbGetAll(STORE_FACES);items.sort((a,b)=>b.createdAt-a.createdAt);faceGallery.innerHTML='';facesEmpty.hidden=items.length>0;for(const item of items){const url=URL.createObjectURL(item.blob),el=document.createElement('button');el.className='face-card';el.innerHTML=`<img src="${url}" alt="Gesichtsbild"><span>${new Date(item.createdAt).toLocaleString('de-DE')}</span>`;el.addEventListener('click',()=>openLightbox(item));faceGallery.appendChild(el)}}let lightboxItem=null;function openLightbox(item){lightboxItem=item;const url=URL.createObjectURL(item.blob);lightboxImg.src=url;lightboxDownload.href=url;lightboxDownload.download=`PersonCam-Gesicht-${formatFilenameStamp(item.createdAt)}.jpg`;lightbox.hidden=false}function closeLightbox(){lightbox.hidden=true;lightboxImg.src=''}closeLightboxBtn.addEventListener('click',closeLightbox);lightbox.addEventListener('click',e=>{if(e.target===lightbox)closeLightbox()});lightboxDelete.addEventListener('click',async()=>{if(!lightboxItem)return;await idbDelete(STORE_FACES,lightboxItem.id);closeLightbox();await renderFaces()});clearVideosBtn.addEventListener('click',async()=>{if(confirm('Alle Aufnahmen löschen?')){await idbClear(STORE_VIDEOS);await renderRecordings()}});clearFacesBtn.addEventListener('click',async()=>{if(confirm('Alle Gesichtsbilder löschen?')){await idbClear(STORE_FACES);await renderFaces()}});tabBtns.forEach(btn=>btn.addEventListener('click',()=>{const tab=btn.dataset.tab;tabBtns.forEach(b=>{const a=b===btn;b.classList.toggle('active',a);b.setAttribute('aria-selected',String(a))});recordingsPanel.hidden=tab!=='recordings';facesPanel.hidden=tab!=='faces'}));function openSettingsSheet(){settingsOverlay.hidden=false}function closeSettingsSheet(){settingsOverlay.hidden=true}settingsBtn.addEventListener('click',openSettingsSheet);closeSettingsBtn.addEventListener('click',closeSettingsSheet);settingsOverlay.addEventListener('click',e=>{if(e.target===settingsOverlay)closeSettingsSheet()});(async function(){loadSettings();await ensurePinProvisioned();pinInput.focus()})();
